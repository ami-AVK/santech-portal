---
const { title, iconID,subtitle, class:className } = Astro.props;
---
<header class:list={["flex gap-2 flex-wrap md:gap-4  md:max-w-none pl-8 md:pl-auto justify-start relative pb-8 items-center",className]}>
    <svg xmlns="http://www.w3.org/2000/svg" class="w-12 stroke-2 md:w-20 text-accent-400" viewBox="0 0 24 24" fill="none">
      <use href={iconID}></use>
    </svg>
  
    <div class="relative flex flex-col tracking-wide text-gray-900 uppercase md:gap-1 ">

      <h2 class="text-2xl font-black leading-6 tracking-wider text-main-900 md:text-4xl">{title}</h2>

      {subtitle?<p class="mt-1 leading-5 text-md md:text-xl">{subtitle}</p>:false}
      
    </div>
</header>