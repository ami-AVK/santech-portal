---
import IconHeader from "../components/IconHeader.astro";

const { title, icon,subtitle } = Astro.props;
---
<div class="flex flex-col self-center pb-64">
    <IconHeader title={title} iconID={icon} subtitle={subtitle}/>
    <slot/>
</div>
